---
description: Inspect the project, run baseline checks, and propose the next roadmap slice.
allowed-tools: Read, Glob, Grep, Bash
---

Read `CLAUDE.md`, `README.md`, and all files in `docs/`. Inspect the source and tests. Run the complete local check suite. Summarize the current implementation honestly, identify the highest-risk gaps, and propose exactly one next vertical slice from `docs/ROADMAP.md` with acceptance tests. Do not modify files.
