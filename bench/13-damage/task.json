{
  "prompt": "make the failing test in check-suite.js pass",
  "verify": [["node", "check.js"]],
  "timeoutSeconds": 900,
  "guard": [
    [
      "node",
      "-e",
      "const t=require('node:fs').readFileSync('check-suite.js','utf8');if(!t.includes('parseAge'))process.exit(1)"
    ]
  ]
}
